services:
  - type: web
    name: cleardeals-backend
    env: python
    plan: free
    pythonVersion: 3.10.12  # ✅ Yeh line ADD kar de
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PORT
        value: 10000
